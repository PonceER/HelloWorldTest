name: Simple Start Up

on:
  push:
    branches: [main]
  schedule:
  - cron: '*****'
jobs:
  name: My Job
  runs-on: ubuntu-latest
   steps:
   - name: Print a greeting
    run:
      echo Hello there!!
